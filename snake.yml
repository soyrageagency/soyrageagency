name: Generate Snake Animation

on:
  schedule:
    # Runs every day at 00:00 UTC
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Generate snake animations
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: soyrageagency
          outputs: |
            dist/github-contribution-grid-snake.svg?color_snake=#F2788C&color_dots=#FFE4E9,#FFB3C0,#F2788C,#D65A6E,#8C2F44
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark&color_snake=#F2788C&color_dots=#161B22,#3D2129,#8C2F44,#F2788C,#FFB454

      - name: Push snake to output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
